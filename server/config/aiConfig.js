// Placeholder for Gemini / OpenAI / any LLM
export const generateAIResponse = async ({ message, context }) => {
  // Replace this later with Gemini SDK
  return `🧠 SenseAI heard: "${message}"`;
};
